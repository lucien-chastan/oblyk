<p class="{{ $align }}">
    <input {{ $onchange }} {{ $value }} class="input-data" name="{{ $name }}" type="checkbox" id="{{ $id }}" {{ $checked }} />
    <label for="{{ $id }}">{{ $label }}</label>
</p>